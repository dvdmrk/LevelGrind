@model MvcLoginRegistration.Models.UserAccount
@{
    ViewBag.Title = "LoggedIn";
}

@if (Session["UserID"] != null)
{
    <h4>Hello @Session["UserName"].ToString() @Session["UserID"]</h4>
}
<p>THR = @((220 - Model.Age) * .7)</p>
